---
updated: 2026-06-27
status: current
maintainer: 项目协作者
role: 单一事实来源 — 新增/删除/重命名 Java 文件时必须同步更新

> 06-27 更新：发动机简化 + GeckoLib 4.6.6 依赖引入 + TestController 验证 SubLevel 渲染兼容性
> 06-27 晚更新：BE 注册中心合并（7→3：ModDebugGear/Swivel/TestController/TurretTest 并入 ModBlockEntityTypes）
---

# 代码文件索引

> 每文件一句话职责。按功能模块分组。

> **📌 历史存档说明**：动力系统（`cockpit/`）和悬挂轮胎系统（`suspension_test/`）自研参数体系的旧版快照已移至 `history/`。
> 06-27 在原位完成发动机简化 + 类拆分重构，自研参数体系已简化到可控范围。
> 旧版快照仅供历史参考，详见 `history/README.md`。

---

## 入口与配置

| 文件 | 职责 |
|------|------|
| `IACP.java` | `@Mod("iac_p")` 主类。注册方块/物品/实体/BE 类型/创造标签页/事件 |
| `IACPClient.java` | 客户端入口。注册配置屏幕、按键映射、客户端事件 |
| `IACPConfig.java` | 🔴 已封存 — 编译时常量配置：SubLevel 缩放开关/系数 |
| `Config.java` | 运行时 ModConfigSpec 配置类：摄像机参数/下车模式/炮塔锚点偏移 |

## 注册中心 (`index/`)

| 文件 | 职责 |
|------|------|
| `ModBlocks.java` | 注册 `SEAT`, `COCKPIT`（下格）, `COCKPIT_UPPER`（上格） |
| `ModItems.java` | 注册 `SEAT` BlockItem, `COCKPIT` BlockItem |
| `ModEntities.java` | 实体类型注册（预留，当前为空壳） |
| `ModCreativeModeTabs.java` | 创造标签页 |
| `ModBlockEntityTypes.java` | 🆕 主注册中心。含 SUSPENSION_TEST, MACHINE_GUN_BASE, SHOTGUN_BASE, DEBUG_GEAR, DEBUG_SWIVEL_BEARING, TEST_CONTROLLER, TURRET_TEST |
| `ModCockpitBlockEntityTypes.java` | 驾驶舱 BE 类型注册（COCKPIT，与普通 BE 分开避免循环依赖） |
| `ModLightCockpitBlockEntityTypes.java` | 轻型座舱 BE 类型注册（COCKPIT_LIGHT，分开避免循环依赖） |
| `ModSounds.java` | 🆕 自定义音效注册表：霰弹枪/机枪开火音效 |

## 游戏内容 (`content/blocks/`)

| 文件 | 职责 |
|------|------|
| `seat/SeatBlock.java` | 初代核心标记方块（半砖形状，保留为模板） |
| `cockpit/CockpitBlock.java` | 驾驶舱下格（炼药锅形状），实现 IBE 接口提供 ticker |
| `cockpit/CockpitUpperBlock.java` | 驾驶舱上格（脚手架形状） |
| `cockpit/CockpitBlockEntity.java` | 驾驶舱 BE：编排 EngineModel/TransmissionModel，状态管理 + NBT + 逐 tick 扫描。tick() 已拆分为 6 个子方法（trySyncStateToClient / tryProcessShifting / tryAutoEngageGear / tryStallRescue / tryAutoDownshift / tryAutoUpshift） |
| `cockpit/PowertrainConstants.java` | 🆕 动力系统编译时常量（06-27 简化后含 TORQUE_MIN/MAX、变速箱齿比、换挡参数） |
| `cockpit/EngineModel.java` | 🆕 发动机计算纯函数（06-27 简化：油门线性扭矩 `TORQUE_MIN + throttle × (TORQUE_MAX - TORQUE_MIN)`，与 RPM 解耦） |
| `cockpit/TransmissionModel.java` | 🆕 变速箱模型纯函数（档位/换挡/转速同步/动力输出分配） |
| `suspension_test/SuspensionTestBlock.java` | 悬挂测试方块（透明模型） |
| `suspension_test/SuspensionTestBlockEntity.java` | 🆕 悬挂 BE：悬挂物理 + 摩擦圆（06-26重新启用）+ 手刹 + 转向 + 按键配置 + 轮胎物理参数系统（胎压/爆胎）。**动态载荷转移**/**Brush 轮胎侧偏模型**/**二次方速度阻尼**已暂禁用。按键映射/手刹/碰撞检测已提取到独立类 ↓ |
| `suspension_test/SmartKeyHandler.java` | 🆕 智能按键映射处理器：手动+智能映射按键存储、NBT 序列化、生效键优先级解析 |
| `suspension_test/BrakeHandler.java` | 🆕 手刹物理处理器：纯函数 `BrakeHandler.compute()`，沿总速度反方向输出滑动摩擦力 |
| `suspension_test/CollisionHandler.java` | 🆕 地形碰撞检测处理器：`rayTerrain()` 射线检测 + `computeMaxExtension()` 最大伸展量计算 |
| `suspension_test/SuspensionConstants.java` | 🆕 悬挂/轮胎/转向全部编译时常量 + 渲染器访问器 |
| `suspension_test/TirePhysicsCalculator.java` | 🆕 轮胎物理纯函数（滚动阻力/爆胎/形变/轮速/阻尼） |
| `suspension_test/BrushTireModel.java` | 🆕 Brush 轮胎侧偏模型纯函数 |
| `suspension_test/SuspensionTestRenderer.java` | 悬挂/轮子自定义渲染器（视觉偏移常量控制，读取 SuspensionConstants） |
| `turret/TurretTestBlock.java` | 🆕 Crossout 风格单块炮塔方块：ENTITYBLOCK_ANIMATED，BlockState 属性 AUTO_ROTATE，右键切换/潜行重置 |
| `turret/TurretTestBlockEntity.java` | 🆕 TurretTest BE：GeoBlockEntity + ComponentHost，yaw/pitch 角度存储 + partialTick 插值 + ClearanceSolver 缓存 + `driveImmediate()` 直接角度驱动 + BE 网络同步 |
| `turret/TurretTestRenderer.java` | 🆕 GeoBlockRenderer：`renderRecursively` 中在 handleAnimations 之后覆盖 yaw/pitch bone 旋转值 |
| `turret/TurretTestModel.java` | 🆕 DefaultedBlockGeoModel 子类：`assets/iac_p/geo/block/turret_test.geo.json` |
| `turret/TurretClearanceSolver.java` | 🆕 网格级炮塔旋转间隙求解器：barrel footprint 旋转矩阵 + 逐角度采样 + 角度范围合并 |
| `turret/ClearanceAlgorithmTest.java` | 🆕 独立测试（无 Minecraft 依赖），9 场景全部通过 |
| `cockpit_light/CockpitLightBlockEntity.java` | 🆕 轻型线性座舱 BE：ComponentHost 实现，基本状态 NBT 持久化，不包含换挡/智能映射/技能系统 |
| `cockpit_light/CockpitLightLinear0Block.java` | 🆕 轻型座舱种子方块（驾驶座），放置触发 2×2 多方块结构 |
| `cockpit_light/CockpitLightLinear1Block.java` | 🆕 轻型座舱水平延伸（副驾区），无对应物品 |
| `cockpit_light/CockpitLightLinear2Block.java` | 🆕 轻型座舱驾驶座上方防滚架，无对应物品 |
| `cockpit_light/CockpitLightLinear3Block.java` | 🆕 轻型座舱副驾上方防滚架，无对应物品 |
| `debug_gear/DebugGearBlock.java` | 🆕 调试小齿轮方块，继承 KineticBlock + ICogWheel |
| `debug_gear/DebugGearBlockEntity.java` | 🆕 调试齿轮 BE：N 键切换每 tick RPM 打印 |
| `debug_swivel/DebugSwivelBearingBlock.java` | 🆕 调试 SwivelBearing 方块 |
| `debug_swivel/DebugSwivelBearingBlockEntity.java` | 🆕 调试 SwivelBearing BE：N 键切换每 tick 状态打印 |
| `test_controller/TestControllerBlock.java` | 🆕 GeckoLib 测试方块（`ENTITYBLOCK_ANIMATED`，RenderShape 委托给 GeoBlockRenderer） |
| `test_controller/TestControllerBlockEntity.java` | 🆕 GeckoLib 测试 BE：实现 GeoBlockEntity，旋转动画 |
| `test_controller/TestControllerRenderer.java` | 🆕 GeoBlockRenderer，6×6×6 渲染包围盒 |
| `test_controller/TestControllerModel.java` | 🆕 DefaultedBlockGeoModel 子类，自动解析 `geo/block/` 路径 |

## 网络 (`network/`)

| 文件 | 方向 | 职责 |
|------|:----:|------|
| `ModNetworking.java` | — | Payload 注册管理器 |
| `SeatMountC2SPacket.java` | C→S | F 键上车/下车请求 |
| `MountedStateS2CPacket.java` | S→C | 骑乘状态同步 + SubLevel UUID + vehicleMass |
| `PlayerInputC2SPacket.java` | C→S | ❌ 已弃用（键盘输入调试） |
| `VehicleControlC2SPacket.java` | C→S | 实时控制输入（throttle/brake/steering） |
| `VehicleKeyConfigC2SPacket.java` | C→S | 自定义按键绑定保存 |
| `SmartMapC2SPacket.java` | C→S | 智能映射：CAR_MODE / REVERSE / TOGGLE_SMART |
| `GearShiftC2SPacket.java` | C→S | Q/E 换挡请求 |
| `GrindstoneConfigC2SPacket.java` | C→S | 砂轮/避雷针 6 方向旋转 |
| `AnchorConfigC2SPacket.java` | C→S | 锚点 A 坐标更新 (x, y, z) |
| `AnchorDataS2CPacket.java` | S→C | 锚点+线条世界坐标推送（手动编解码，16 字段） |
| `MachineGunTargetC2SPacket.java` | C→S | 🆕 机枪/炮塔瞄准数据：命中点世界坐标 (hitX,hitY,hitZ) + 摄像机实际朝向 (cameraYaw,cameraPitch)，packet handler 直接驱动消除延迟 |
| `WeaponFireC2SPacket.java` | C→S | 开火命中坐标 (hitX, hitY, hitZ) + 射线起点 (originX,originY,originZ)，服务端造成 2 点伤害（无视无敌帧）+ 部件损坏入口 |
| `DebugGearToggleC2SPacket.java` | C→S | 🆕 调试齿轮 N 键切换：通知服务端切换 DebugGearBlockEntity 调试输出开关 |
| `DebugSwivelToggleC2SPacket.java` | C→S | 🆕 调试 SwivelBearing N 键切换：通知服务端切换 DebugSwivelBearingBlockEntity 调试输出开关 |
| `TireConfigC2SPacket.java` | C→S | 🆕 胎压配置：发送 (BlockPos, nominalPressure) 到服务端写入悬挂 BE NBT |
| `VehicleStateS2CPacket.java` | S→C | 🆕 载具实时状态同步：发动机RPM/油门/档位/熄火/扭矩/速度/加速度，每2 tick推送 |
| `WeaponSoundS2CPacket.java` | S→C | 🆕 武器开火音效播放同步（发送给除开火者外所有玩家） |

## 事件 (`events/`)

| 文件 | 职责 |
|------|------|
| `PlayerMountTracker.java` | 骑乘状态表、SubLevel 占用表、驾驶舱扫描检测、服务端每 tick 同步、归属注册/注销、断线/死亡/SubLevel 销毁正确清理 |
| `ServerMountHandler.java` | 上车/下车逻辑、条件检查（唯一性/独占/结构完整） |
| `SubLevelScanner.java` | 🆕 SubLevel chunk 遍历工具类，统一 boilerplate 消除三重嵌套循环 |
| `MountedProtectionHandler.java` | 🆕 骑乘玩家伤害免疫 + 仇恨屏蔽，NeoForge 总线注册 |
| `SubLevelOwnership.java` | 🆕 SubLevel 归属管理系统（已委派到 AffiliationRegistry，保持向后兼容） |
| `SableBlockHelper.java` | 🆕 SubLevel 方块查询工具：`findSubLevelAt()` 单点查询 + `rayTraceSubLevels()` 射线追踪（已缓存 resolvePolicy 避免重复调用）+ RayType 重载 |

## 技能系统 (`skill/`)

| 文件 | 职责 |
|------|------|
| `SkillRegistry.java` | 🆕 驾驶技能注册表单例：管理技能加载/注册/查询，内置默认技能（car_mode） |
| `DrivingSkill.java` | 🆕 驾驶技能数据模型：JSON 反序列化，定义 WASD+QE 到各悬挂组的映射规则 |
| `SkillExpr.java` | 🆕 技能表达式引擎：解析和求值技能中的输入映射表达式 |
| `PartDamageCache.java` | 🆕 部件损坏管理器（简化版）：`damageBlock(Level, Vec3, float)` 单入口，`findSubLevelAt()` 直接查找命中点，5 击摧毁 + 裂纹同步。不做归属排除一视同仁 |
| `BlockDamageSavedData.java` | 🆕 方块破坏进度 NBT 持久化（SavedData），世界加载时恢复裂纹，确保重启后数据不丢失 |
| `PartData.java` | ❌ 已废弃（保留空壳，耐久数据已内联到 PartDamageCache） |
| `SubLevelProjectileHandler.java` | 🆕 弹射物进入 SubLevel 检测：监 hearing EntityTickEvent.Post 触发部件损坏 |

## 客户端 (`client/`)

| 文件 | 职责 |
|------|------|
| `ClientEvents.java` | F 键上车/下车、C 键配置界面/朝向信息界面、WASD 输入检测（改用 getActiveKey*()）、Q/E 换挡检测、鼠标左键按住连发、持续瞄准、骑乘时玩家渲染取消 |
| `ClientMountHandler.java` | 骑乘状态管理、玩家同步/隐藏、质量缓存、自适应摄像机数据源、**scanOrientation()** 悬挂扫描缓存、smartMappingActive 缓存 |
| `ClientMountGameHandler.java` | 骑乘时交互禁止（点击/破坏/放置/手部渲染）+ 断线重连 NBT 清理 |
| `VehicleDebugOverlay.java` | 调试信息覆盖层（RenderGuiEvent.Post，右下角 F3 风格） |
| `screen/GrindstoneConfigScreen.java` | 炮塔部件配置界面（C 键）：左列 6 方向按钮 + 右列锚点 X/Y/Z 编辑框 |
| `screen/VehicleKeyConfigScreen.java` | 🆕 按键配置界面：胎压输入 + 智能映射键回退显示（绿色高亮） |
| `screen/VehicleOrientationScreen.java` | WASD 智能映射 GUI：FACING 统计 + 汽车模式/反转/开关按钮 |
| `VehicleOrientationData.java` | 记录 SubLevel 悬挂 HORIZONTAL_FACING 分布 (N/S/E/W)，推断宽度轴/前进轴 |
| `WeaponOverlay.java` | 武器系统覆盖层：上车时屏幕中央 2×2 准星渲染 + 多炮台炮口射线（fireAllTurrets）+ 方块面中心瞄准 + 弹道数据缓存 + 通用射线检测（raycastGeneric）+ 准星偏向锥角 |
| `StructureInfoData.java` | 🆕 结构信息数据：SubLevel 内方块统计、武器系统概览与底座连接信息 |
| `screen/StructureInfoScreen.java` | 🆕 结构概览界面（N 键打开）：方块种类/数量统计、武器系统装配状态 |
| `renderer/BulletTrailRenderer.java` | 弹道渲染（Veil 修复）：AFTER_LEVEL + RenderType.LINES + 摄像机相对坐标 + endLastBatch()。炮口位置每帧 renderPose(partialTick) 动态计算消除抖动 |
| `renderer/AxisLineRenderer.java` | 三色轴线渲染：默认隐藏，仅 F3+B（显示碰撞箱）时可见 |
| `test_controller/TestControllerRenderer.java` | 🆕 GeckoLib GeoBlockRenderer：6×6×6 渲染包围盒，旋转动画，验证 SubLevel 内渲染兼容性 |

## 归属系统 (`affiliation/`)

| 文件 | 职责 |
|------|------|
| `AffiliationTag.java` | 🆕 归属身份证 record：ownerId/vehicleId/groupId/role/faction，NBT 序列化 |
| `AffiliationRole.java` | 🆕 角色枚举：VEHICLE_BODY/TURRET_BASE/TURRET_YAW/TURRET_PITCH 等 |
| `AffiliationRegistry.java` | 🆕 集中运行时索引：SubLevel→Tag, Group→成员, 玩家→载具；含 RayPolicy 策略矩阵 + 性能监控 |
| `AffiliationChangeEvent.java` | 🆕 归属变更事件：注册/注销/整组清理/玩家绑定时抛出 |
| `AffiliationCommand.java` | 🆕 归属调试命令：`/iacp affiliation list|check|verify|slowquery|player` |
| `AffiliationHelper.java` | 🆕 归属工具方法：便捷注册/注销载具主体和炮塔部件 |
| `ComponentRegistry.java` | 🆕 部件注册表：三层索引（bySubLevel/byRole/posToSub），BE onLoad 自动注册 |
| `ComponentHost.java` | 🆕 部件宿主接口：CockpitBE/SuspensionBE/TurretBaseBE 实现 onLoad 自动注册/onChunkUnloaded 注销 |
| `ComponentEntry.java` | 🆕 部件条目 record：blockEntity/SubLevelUUID/pos/role，NBT 序列化方法 |
| `ComponentRole.java` | 🆕 部件角色枚举：COCKPIT/SUSPENSION/TURRET_BASE/TURRET_TEST/DEBUG_GEAR 等 |
| `RayPolicy.java` | 🆕 射线交互策略矩阵：CAMERA_AIM/WEAPON_DAMAGE 自动排除自伤 |
| `RayType.java` | 🆕 射线类型枚举：CAMERA_AIM/WEAPON_DAMAGE/PROJECTILE 等 |
| `WorldLoadHandler.java` | 🆕 世界加载事件处理器：监听 `LevelEvent.Load`，清空 AffiliationRegistry + ComponentRegistry，等待 NBT 重建 |

## Mixin (`mixin/`)

| 文件 | 注入点 | 职责 |
|------|--------|------|
| `CameraMixin.java` | `Camera.setup()` @Tail | 轨道摄像机：球面环绕 SubLevel 焦点，自适应高度/距离，极点奇点修复 |

> `PlayerModelRotationMixin` 已于 06-05 废弃删除——SubLevel 四元数 yaw 与 yBodyRot 存在不可调和的双重叠加冲突。

---

## 📦 历史存档（`history/`）

> 06-27 前自研参数体系的旧版快照。仅作历史参考，不参与编译。

| 模块 | 原始路径 | 说明 |
|------|----------|------|
| 驾驶舱动力系统（旧版） | `content/blocks/cockpit/` | 旧版发动机模型、扭矩曲线、连续油门、质量自适应扭矩等（06-27 简化前） |
| 悬挂/轮胎物理（旧版） | `content/blocks/suspension_test/` | 旧版弹簧阻尼器、BinaryGrip、胎压、Brush侧偏（06-26 封存前） |

**存档原因**：自研参数体系耦合严重（~30+ 互相影响的常量），简化后旧版移至 `history/`。
